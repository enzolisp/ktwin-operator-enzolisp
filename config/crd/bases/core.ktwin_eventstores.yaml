---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: eventstores.core.ktwin
spec:
  group: core.ktwin
  names:
    kind: EventStore
    listKind: EventStoreList
    plural: eventstores
    singular: eventstore
  scope: Namespaced
  versions:
  - name: v0
    schema:
      openAPIV3Schema:
        description: EventStore is the Schema for the eventstores API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EventStoreSpec defines the desired state of EventStore
            properties:
              autoScaling:
                properties:
                  maxScale:
                    type: integer
                  metric:
                    description: 'KNative Metric values (default, if not informed:
                      concurrency) concurrency: the number of simultaneous requests
                      that can be processed by each replica of an application at any
                      given time rps: requests per seconds cpu: cpu usage memory:
                      memory usage'
                    type: string
                  minScale:
                    type: integer
                  parallelism:
                    type: integer
                  target:
                    type: integer
                  targetUtilizationPercentage:
                    type: integer
                type: object
            type: object
          status:
            description: EventStoreStatus defines the observed state of EventStore
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
